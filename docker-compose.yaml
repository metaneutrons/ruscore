services:
  ruscore:
    build: .
    ports:
      - "3000:3000"
    environment:
      - RUSCORE_PORT=3000
      - RUSCORE_DATA_DIR=/home/ruscore/data
      - RUST_LOG=info
    volumes:
      - ruscore-data:/home/ruscore/data
    restart: unless-stopped

volumes:
  ruscore-data:
